node_modules/
dist/
coverage/
.DS_Store
*.wasm
.prompt-*.md
kernel/target/
kernel/Cargo.lock
# The generated WASM package is checked in: it is what the app ships, and a
# clone without a Rust toolchain still has to type-check, build and test.
!kernel/tectonic-wasm/pkg/*.wasm
