Fix the 1 failing test in tests/app/EditorView.test.tsx. The test "shows the document name and the feature tree" is failing because the assertion doesn't match the actual rendered output. Read the test file and the EditorView.tsx source, run npm test to see the failure, then fix the assertion to match what the component actually renders. Only change the test file, not the source code. After fixing, run npm test and confirm all tests pass.